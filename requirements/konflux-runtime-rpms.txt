# OS packages for the Konflux-pin python CI job only (live DNF).
# python3.12 / python3.12-pip are skipped; setup-python provides those.
#
# Not used by Dockerfile.konflux. The image stays on UBI packages from
# requirements/rpms.in.yaml. re2 and thrift come from EPEL 9 so AIPCC
# pyarrow==20.0.0 can load in CI; snappy comes from CentOS Stream 9 BaseOS
# (UBI omits it). The workflow enables those repos at job time.

freetype
libgomp
libjpeg-turbo
libtiff
openblas-threads
openjpeg2
postgresql-libs
python3.12
python3.12-pip
re2
snappy
sqlite
thrift
utf8proc
